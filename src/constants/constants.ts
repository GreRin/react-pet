import { environment } from '../environments/environment';

export const LS_FAV_KEY = 'rfk';
export const ACCESS_TOKEN_KEY = 'accessToken';
export const GRAPHQL_URL = `${environment.baseUrl}/graphql`;
